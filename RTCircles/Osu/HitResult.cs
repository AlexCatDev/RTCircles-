namespace RTCircles
{
    //Would be cool instead of doing it like osu,
    //Score hits based on actual accuracy and then scale the hitresult by how accurate the hit is
    public enum HitResult
    {
        Max = 300,
        Good = 100,
        Meh = 50,
        Miss = 0
    }
}
